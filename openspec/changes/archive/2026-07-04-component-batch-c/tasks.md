# component-batch-c — tasks

- [x] 1. Author full P1 component trees for expressive, creative-multiscale per the matrix rows
     (exact registry path shape, alias-first).
- [x] 2. Append expressive, creative-multiscale to COMPONENT_P1_ROLLOUT (append-only).
- [x] 3. golden/component-c.test.ts: parity acceptance, specimen
     completeness, tokenHash pins per recipe.
- [x] 4. Full-parity assertion across all 8 recipes.
- [x] 5. npm test green + tsc clean.
