# component-batch-c — tasks

- [ ] 1. Author full P1 component trees for expressive, creative-multiscale per the matrix rows
     (exact registry path shape, alias-first).
- [ ] 2. Append expressive, creative-multiscale to COMPONENT_P1_ROLLOUT (append-only).
- [ ] 3. golden/component-c.test.ts: parity acceptance, specimen
     completeness, tokenHash pins per recipe.
- [ ] 4. Full-parity assertion across all 8 recipes.
- [ ] 5. npm test green + tsc clean.
